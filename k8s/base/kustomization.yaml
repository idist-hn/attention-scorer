apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: attention-detection

resources:
  - namespace.yaml
  - configmap.yaml
  - secrets.yaml
  - postgres.yaml
  - redis.yaml
  - face-detection.yaml
  - landmark-detection.yaml
  - head-pose.yaml
  - gaze-tracking.yaml
  - blink-detection.yaml
  - attention-scorer.yaml
  - pipeline-orchestrator.yaml
  - api-gateway.yaml
  - web-dashboard.yaml
  - ingress.yaml
  # - hpa.yaml  # Disabled - using 1 replica per deployment
  # - network-policy.yaml  # Disabled - was blocking ingress traffic
  # - pod-security.yaml  # Disabled - PodSecurityPolicy deprecated

commonLabels:
  app.kubernetes.io/name: attention-detection
  app.kubernetes.io/version: "1.0.0"
  app.kubernetes.io/managed-by: kustomize

