apiVersion: v1
kind: Secret
metadata:
  name: attention-secrets
  namespace: attention-detection
type: Opaque
stringData:
  # Database credentials
  postgres-user: "postgres"
  postgres-password: "postgres123"
  DATABASE_USER: "postgres"
  DATABASE_PASSWORD: "postgres123"
  DATABASE_URL: "postgresql://postgres:postgres123@postgres.attention-detection.svc.cluster.local:5432/attention_db?sslmode=disable"
  DB_USER: "postgres"
  DB_PASSWORD: "postgres123"

  # Redis password
  REDIS_PASSWORD: ""

  # JWT secret for API Gateway
  JWT_SECRET: "attention-k8s-jwt-secret-2024-production"

  # API keys
  API_KEY: "attention-api-key-2024"

