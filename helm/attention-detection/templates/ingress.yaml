{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "attention-detection.fullname" . }}
  labels:
    {{- include "attention-detection.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                {{- if eq .service "web-dashboard" }}
                name: {{ include "attention-detection.fullname" $ }}-web-dashboard
                port:
                  number: {{ $.Values.webDashboard.service.port }}
                {{- else if eq .service "api-gateway" }}
                name: {{ include "attention-detection.fullname" $ }}-api-gateway
                port:
                  number: {{ $.Values.apiGateway.service.port }}
                {{- end }}
          {{- end }}
    {{- end }}
{{- end }}

